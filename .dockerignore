# Version control
.git
.github

# Dependencies — reinstalled during the Docker build
node_modules
vendor

# Local FrankenPHP binary (macOS) — the Docker image uses its own
frankenphp

# Frontend build output — generated during the Docker build (frontend stage)
public/build

# Local environment files — secrets must never end up in the image
.env
.env.*
!.env.example

# Laravel runtime artifacts — not needed at build time
storage/logs/*.log
storage/framework/cache/data/*
storage/framework/sessions/*
storage/framework/views/*
bootstrap/cache/*.php

# Dev tooling
tests/
.github/
pint.json
phpunit.xml
AGENTS.md
DEPLOYMENT.md
*.md
